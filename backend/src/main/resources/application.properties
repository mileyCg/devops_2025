# Cloud SQL Configuration
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/goaltracker

spring.datasource.username=devops
spring.datasource.password=devops123!
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# H2 Database Configuration (Commented out)
# spring.datasource.url=jdbc:h2:mem:goaltracker
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password

# H2 Console (for development) (Commented out)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# JPA Configuration (Commented out for H2)
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Enable debug logging for Cloud SQL


# Set credentials scope


# Server Configuration
server.port=8080

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:4200,http://34.60.163.229:80,http://frontend-service:80,http://frontend-service.default.svc.cluster.local:80
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
