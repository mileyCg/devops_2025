apiVersion: apps/v1
kind: Deployment
metadata:
  name: goal-tracker-backend
  namespace: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: goal-tracker-backend
  template:
    metadata:
      labels:
        app: goal-tracker-backend
    spec:
      containers:
      - name: backend
        image: gcr.io/PROJECT_ID/goal-tracker-backend:staging-SHA
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "staging"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://staging-db:3306/goaltracker"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/goals/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/goals/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: staging
spec:
  selector:
    app: goal-tracker-backend
  ports:
  - port: 80
    targetPort: 8080
  type: LoadBalancer
